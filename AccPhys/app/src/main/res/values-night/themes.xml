<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base Theme - reuse and customize Material3 base -->
    <style name="Base.Theme.AccPhys" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Color palette -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/white</item>

        <item name="colorTertiary">@color/colorTertiary</item>
        <item name="colorOnTertiary">@color/white</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Application Theme -->
    <style name="Theme.AccPhys" parent="Base.Theme.AccPhys">
        <!-- System bar styling -->
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- Light status bar icons/text if using a light theme -->
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
